public class Main {
    public static void main(String[] args) {
        long InitialBalance = 100_00; //including kopecks
        long SumReplenish = 1000_01; //including kopecks
        long Bonus;
        if (SumReplenish > 1000_00) {
            Bonus = SumReplenish / 100; //including kopecks
        } else {
            Bonus = 0_00;
        }
        long TotalBalance = InitialBalance + SumReplenish + Bonus; //including kopecks
        System.out.println(TotalBalance);
    }
}
